# Templates

本目录包含 7 份完整的 VSP-Beamer 模板，覆盖教学、正式报告、上海科技大学和奶龙视觉变体。所有文件均可通过根目录 Makefile 独立编译：

```bash
make render INPUT=templates/tutorial-red.tex OUTPUT=/tmp/tutorial-red.pdf
```

模板是正常、可直接编辑的 Beamer 源文件。
